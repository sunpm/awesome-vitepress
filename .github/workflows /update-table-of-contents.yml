name: 'Update Table of Contents'

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  generateTOC:
    name: TOC Generator
    runs-on: ubuntu-latest
    steps:
      - uses: technote-space/toc-generator@v4
